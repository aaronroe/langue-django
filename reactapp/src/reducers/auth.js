import Const from '../constants';


const defaultState = {
  error: null,
};


export default function auth(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
};
